import { Box } from "@chakra-ui/react";

import AddContactDialog from "./addContactDialog";

const Sidebar = () => {
  return (
    <Box>
      <AddContactDialog />
    </Box>
  );
};

export default Sidebar;
